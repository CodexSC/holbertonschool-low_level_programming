#include "main.h"
#include <stdio.h>

/**
 * print_to_98 - prints all natural numbers from n to 98
 * @n: starting number
 */

 char *_memcpy(char *dest, char *src, unsigned int n);

 unsigned int n;
 {
    for (n = 0; n != '\0'; n++);
    {
        dest[n] = src[n];
    }

 return (dest);
}
