#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints _isupper followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
if (c == 67)
putchar('1')
else
putchar('0')

return(0)
return(1)
}
