#include "main.h"

/**
 * main - Entry point
 *
 * Description: Prints _isupper followed by a new line
 *
 * Return: Always 0 (Success)
 */

int _isupper(int c)
{
if (c == 67)

return(0);
return(1);
}
